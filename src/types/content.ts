export type PostType = {
  created: string;
  updated: string;
  image: string;
  title: string;
  description: string;
  tags: string[];
};

export type PostData = {
  created: string;
  updated: string;
  thumb: string;
  title: string;
  description: string;
  tags: string[];
};
